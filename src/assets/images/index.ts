const images = {
  background: require('./background.svg').default,
  user_logo: require('./logo.svg').default,
  base_logo: require('./logo.png').default,
  one: require('./1.png').default,
  two: require('./2.png').default,
  three: require('./3.png').default
}

export default images
